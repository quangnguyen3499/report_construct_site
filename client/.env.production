# Production environment variables for the frontend
# Set to false so production builds use the real backend
VITE_USE_MOCK_DATA=false
# Replace with your backend API URL (no trailing /api)
VITE_API_BASE_URL=https://report-construct-site.onrender.com/api
